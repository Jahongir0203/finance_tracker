import 'package:finance_tracker/core/extensions/string_extension.dart';
import 'package:finance_tracker/features/common/presentation/mixins/expense_categories_mixin.dart';

import '../../../../../exports.dart';

//TODO implement
/*
class IncomeCategoriesPage extends StatelessWidget with ExpenseCategoriesMixin {


  @override
  Widget build(BuildContext context) {
    return WLayout(
      child: Scaffold(
        appBar: PreferredSize(
          preferredSize: Size.fromHeight(56),
          child: WAppBar(title: "Daromadlar kategoriyasi"),
        ),
        body: ListView.separated(
          shrinkWrap: true,
          scrollDirection: Axis.vertical,
          keyboardDismissBehavior: ScrollViewKeyboardDismissBehavior.onDrag,
          itemBuilder: (context, index) {
            final data = [index];
            return ListTile(leading: Text(data.i));
          },
          separatorBuilder: (context, index) => 8.verticalSpace,
          itemCount: incomes.length,
        ),
      ),
    );
  }
}
*/
